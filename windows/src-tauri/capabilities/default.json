{
  "$schema": "https://schema.tauri.app/config/capability",
  "identifier": "default",
  "description": "Default permissions for SyncBeats Windows App",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open"
  ]
}
